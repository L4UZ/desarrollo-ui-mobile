import * as React from 'react';
import { View, Text } from 'react-native';

const SigninScreen = () => {
  return (
    <View>
      <Text>Signin index</Text>
    </View>
  );
};

export default SigninScreen;
